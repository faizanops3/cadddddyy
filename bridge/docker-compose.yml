version: "3.9"
services:
  bridge:
    build: .
    container_name: exotel-ultravox-bridge
    restart: unless-stopped
    environment:
      ULTRAVOX_WS_URL: "${ULTRAVOX_WS_URL}"
      ULTRAVOX_API_KEY: "${ULTRAVOX_API_KEY}"
    network_mode: "host" # so it binds 0.0.0.0:8080 on the VM
